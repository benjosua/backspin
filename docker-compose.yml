services:
  backspin:
    build: .
    image: backspin:latest
    ports:
      - "2567:2567"
    environment:
      NODE_ENV: production
      PORT: 2567
      CLIENT_DIST_DIR: /app/dist
    restart: unless-stopped
